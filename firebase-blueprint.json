{
  "entities": {
    "User": {
      "title": "User",
      "description": "User profile information",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "role": { "type": "string", "enum": ["admin", "user"] },
        "displayName": { "type": "string" },
        "birthDate": { "type": "string", "format": "date" },
        "phone": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "approved", "rejected"] }
      },
      "required": ["uid", "email", "role", "status"]
    },
    "Game": {
      "title": "Game",
      "description": "A football match in the tournament",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "team1": { "type": "string" },
        "team2": { "type": "string" },
        "date": { "type": "string", "format": "date-time" },
        "result": { "type": "string", "enum": ["win1", "draw", "win2", "pending"] },
        "order": { "type": "integer" }
      },
      "required": ["team1", "team2", "result"]
    },
    "Bet": {
      "title": "Bet",
      "description": "A user's prediction for a game",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "gameId": { "type": "string" },
        "prediction": { "type": "string", "enum": ["win1", "draw", "win2"] },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "gameId", "prediction"]
    },
    "BolaoData": {
      "title": "BolaoData",
      "description": "Global application configuration",
      "type": "object",
      "properties": {
        "pass": { "type": "string" },
        "t1": { "type": "string" },
        "t2": { "type": "string" },
        "isAuditReady": { "type": "boolean" }
      }
    },
    "Cartela": {
      "title": "Cartela",
      "description": "A set of bets with payment status",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "userName": { "type": "string" },
        "predictions": { "type": "object" },
        "paymentStatus": { "type": "string", "enum": ["pending", "approved", "rejected"] },
        "timestamp": { "type": "string", "format": "date-time" },
        "quantity": { "type": "integer" },
        "totalAmount": { "type": "number" }
      },
      "required": ["userId", "predictions", "paymentStatus", "quantity", "totalAmount"]
    },
    "Team": {
      "title": "Team",
      "description": "A team with its name and logo",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "logo": { "type": "string" }
      },
      "required": ["name", "logo"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "User profiles"
    },
    "/games/{gameId}": {
      "schema": "Game",
      "description": "Tournament games"
    },
    "/draftGames/{gameId}": {
      "schema": "Game",
      "description": "Games in preparation (draft)"
    },
    "/bets/{betId}": {
      "schema": "Bet",
      "description": "User predictions"
    },
    "/cartelas/{cartelaId}": {
      "schema": "Cartela",
      "description": "User cartelas and payments"
    },
    "/config/main": {
      "schema": "BolaoData",
      "description": "Global application configuration"
    },
    "/teams/{teamId}": {
      "schema": "Team",
      "description": "Team logos and names"
    }
  }
}
